/*
* Import Wordpress dependencies
*/
const {
	InspectorControls,
	MediaPlaceholder
} = wp.editor;
const {
  ColorPalette,
  IconButton,
  Panel,
  PanelBody,
  PanelRow
} = wp.components;
 const { withState } = wp.compose;



export default CustomInspector
