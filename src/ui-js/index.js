// var block = $( 'div.wp-block' )
//
// var observer = new MutationObserver( function( mutations ) {
// 	mutations.forEach( function( mutation ) {
// 		if ( !mutation.addedNodes )
// 			return;
//
// 		for ( var i = 0; i < mutation.addedNodes.length; i++ ) {
// 			if ( ! ( mutation.target.className.startsWith( 'wp-block' ) || mutation.target.className.startsWith( 'jonasm-slider' ) ) )
// 				return;
// 			return slider = mutation;
// 		}
// 	} )
// } )
//
// observer.observe( document.body, {
// 	childList: true,
// 	subtree: true,
// 	attributes: false,
// 	characterData: false
// } )
//
// // stop watching using:
// // observer.disconnect()
